import { EventBlokerDirective } from './event-bloker.directive';

describe('EventBlokerDirective', () => {
  it('should create an instance', () => {
    const directive = new EventBlokerDirective();
    expect(directive).toBeTruthy();
  });
});
