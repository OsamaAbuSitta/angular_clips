import { FTimestampPipe } from './f-timestamp.pipe';

describe('FTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new FTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
